import HoverPersonInfoDirective from './hover-person-info.directive';
/*
describe('HoverPersonInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverPersonInfoDirective();

    expect(directive).toBeTruthy();
  });
}); */
